{
  "name": "cloudflare-ai-api",
  "version": "1.0.0",
  "description": "API con Workers AI y memoria de conversación",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
